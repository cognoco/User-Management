# 2026-03-02 Cron: UM Cleanup

## useApiKeys.test.tsx
- Fixed 2 TS errors: test was importing `ApiKey` from old `types.ts`, needed `models.ts`
- Updated test data to match `models.ts` shape (userId, prefix, scopes, isRevoked, string dates)
- Updated createApiKey test to use `ApiKeyCreateResult` (success/key/plaintext) instead of `ApiKey & { key }`
- Tests pass ✅
- Total errors: 184 (excluding .next/ noise), down from 186
