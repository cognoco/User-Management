// Server-only re-exports for runtime configuration utilities.
// Import this module from API routes, services, and server utilities only.
export { getConfig, getClientConfig, getServerConfig } from '@/core/config/runtime-config';

